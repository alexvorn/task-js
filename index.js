//parth 2
var calculator = {
    read : function ( x, y ) {
        x = prompt(),
        y = prompt();
        return (x, y)
    },
    sum : function ( x, y ) {
          this.x + this.y 
    }
};

calculator.read();
console.log(calculator.sum());

//parth 3
// var user = {
//     name : "Tom"
// };

// function format(beginMsg, endMsg) {
//     console.log(beginMsg + this.name + endMsg);
// }

// var tomFormat = 